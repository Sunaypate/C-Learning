#include <stdio.h>

int main() {
    int count = 0;
    // while (count < 50) {
    //     printf("Curr Count: %d\n", count);
    //     count++;
    // }

    for (int i = 0; i < 50; i++) {
        printf("Curr Count: %d\n", i);
    }
    return 0;
}