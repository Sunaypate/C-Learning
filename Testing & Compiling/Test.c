#include <stdio.h>
#include <stdlib.h>

int main() {
    printf("Lets friggin go!\n");
    system("pause");
    return 0;
}

/*
To run files from terminal,
type "gcc filename"
then type .\a

If using vscode run button, a pause is required inorder to view terminal
before it closes immediately. This system pause needs "#include <stdlib.h>"
in order to work.
*/